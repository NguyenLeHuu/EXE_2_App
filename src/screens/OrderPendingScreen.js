import React from "react";
import {
  View,
  Text,
  StatusBar,
  TouchableOpacity,
  SafeAreaView,
  TextInput,
  Image,
  StyleSheet,
  Button,
} from "react-native";
import { COLORS, SIZES } from "../constants/index";
const OrderPendingScreen = ({ navigation }) => {
  return (
    <>
      <Text>pending</Text>
    </>
  );
};

export default OrderPendingScreen;
