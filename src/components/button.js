import React from "react";
import {
  View,
  Text,
  StatusBar,
  TouchableOpacity,
  SafeAreaView,
  TextInput,
  Image,
  StyleSheet,
  Button,
  Pressable,
} from "react-native";
const Button = ({ sportField }) => {
  return (
    <TouchableOpacity
      style={{
        height: 60,
        width: 330,
        margin: 12,
        borderWidth: 1,
        padding: 10,
        backgroundColor: COLORS.primary,
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <Text>hhh</Text>
    </TouchableOpacity>
  );
};

export default Button;
